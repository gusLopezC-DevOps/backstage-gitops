apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: workloads
  labels:
    app: backstage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backstage
  template:
    metadata:
      labels:
        app: backstage
    spec:
      serviceAccountName: backstage
      initContainers:
      - name: wait-for-postgres
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z postgres 5432; do echo waiting for postgres; sleep 2; done;']
      - name: copy-assets
        image: busybox:1.36
        command:
        - sh
        - -c
        - |
          mkdir -p /store-templates /store-plugins /store-config
          while read -r key dst; do
            case "$key" in ''|\#*) continue;; esac
            case "$dst" in
              templates/*) base=/store-templates; rel=${dst#templates/};;
              plugins/*)   base=/store-plugins;   rel=${dst#plugins/};;
              *)           base=/store-config;    rel=$dst;;
            esac
            mkdir -p "$base/$(dirname "$rel")"
            cp "/cfg/$key" "$base/$rel"
          done < /cfg/layout
        volumeMounts:
        - name: backstage-templates
          mountPath: /cfg
        - name: store-templates
          mountPath: /store-templates
        - name: store-plugins
          mountPath: /store-plugins
        - name: store-config
          mountPath: /store-config
      containers:
      - name: backstage
        image: docker.io/guslopezc/backstage:v14
        imagePullPolicy: Always
        ports:
        - containerPort: 7007
        env:
          - name: GITHUB_TOKEN
            valueFrom:
              secretKeyRef:
                name: backstage-secrets
                key: GITHUB_TOKEN
          - name: AUTH_GITHUB_CLIENT_ID
            valueFrom:
              secretKeyRef:
                name: backstage-secrets
                key: AUTH_GITHUB_CLIENT_ID
          - name: AUTH_GITHUB_CLIENT_SECRET
            valueFrom:
              secretKeyRef:
                name: backstage-secrets
                key: AUTH_GITHUB_CLIENT_SECRET
          - name: POSTGRES_HOST
            value: postgres.workloads.svc.cluster.local
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: postgres-sealed
                key: POSTGRES_USER
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: postgres-sealed
                key: POSTGRES_PASSWORD
          - name: NODE_OPTIONS
            value: "--no-node-snapshot"
        args:
          - node
          - packages/backend
          - --config
          - app-config.yaml
          - --config
          - app-config.production.yaml
          - --config
          - /app/config/override.yaml
        readinessProbe:
          httpGet:
            path: /
            port: 7007
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 7007
          initialDelaySeconds: 60
          periodSeconds: 15
        volumeMounts:
        - mountPath: /app/templates
          name: store-templates
        - mountPath: /app/plugins
          name: store-plugins
        - mountPath: /app/config
          name: store-config
        resources:
          requests:
            memory: 512Mi
            cpu: 250m
          limits:
            memory: 1Gi
            cpu: 1000m
      volumes:
      - name: backstage-templates
        configMap:
          name: backstage-templates
          defaultMode: 420
      - name: store-templates
        emptyDir: {}
      - name: store-plugins
        emptyDir: {}
      - name: store-config
        emptyDir: {}
