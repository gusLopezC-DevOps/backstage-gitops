apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${{ values.repoName }}
  namespace: ${{ values.repoName }}
  labels:
    app: ${{ values.repoName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${{ values.repoName }}
  template:
    metadata:
      labels:
        app: ${{ values.repoName }}
    spec:
      containers:
        - name: app
          image: docker.io/guslopezc/${{ values.repoName }}:ci
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi