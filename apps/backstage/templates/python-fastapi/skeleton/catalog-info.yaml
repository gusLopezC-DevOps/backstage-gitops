apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ repoName }}
  description: ${{ description }}
  annotations:
    github.com/project-slug: gusLopezC-DevOps/${{ repoName }}
    backstage.io/kubernetes-id: ${{ repoName }}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: production
  owner: group:default/${{ grupo }}