apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${{ parameters.repoName }}
  namespace: ${{ parameters.repoName }}
spec:
  ingressClassName: kong
  rules:
    - host: ${{ parameters.repoName }}.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ${{ parameters.repoName }}
                port:
                  name: http