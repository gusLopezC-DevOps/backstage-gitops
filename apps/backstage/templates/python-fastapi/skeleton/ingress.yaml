apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${{ repoName }}
  namespace: ${{ repoName }}
spec:
  ingressClassName: kong
  rules:
    - host: ${{ repoName }}.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ${{ repoName }}
                port:
                  name: http