apiVersion: v1
kind: Service
metadata:
  name: ${{ repoName }}
  namespace: ${{ repoName }}
spec:
  selector:
    app: ${{ repoName }}
  ports:
    - name: http
      port: 8000
      targetPort: 8000